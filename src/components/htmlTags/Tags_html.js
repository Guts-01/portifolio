import styled from 'styled-components';

export const HtmlTag_hr = styled.hr`
    border: 1px solid var(--corSectionsLinks);
    width: 100%;
`;

export const HtmlTag_a = styled.a`
    color: var(--corTexto);
    text-decoration: none;
    font-family: var(--fontPrincipal);
`;

export const HtmlTag_p = styled.p`
    font-family: var(--fontPrincipal);
`;

export const HtmlTag_h1 = styled.h1`
    color: var(--corTexto);
    font-family: var(--fontPrincipal);
    font-size: 2.2em;
`;

export const HtmlTag_br = styled.br`
    display: block;
    margin-top: 3px;
    margin-bottom: 2px;
`;

export const HtmlTag_space_columm = styled.div`
    display: block;
    margin-top: 10px;
    margin-bottom: 5px;
`;
